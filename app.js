'use strict';
// Seattle 
function getRanNum(a, b,) {
  return Math.random() * (a - b);
  ;
}
console.log(Math.random() * (65 - 23) + 23);

// console.log(getRanNum(65, 23));

// // Tokyo
function getRanNum(a, b,) {
  return Math.random() * (a - b);
  ;
}
console.log(Math.random() * (24 - 3) + 3);

// // Dubai 
function getRanNum(a, b,) {
  return Math.random() * (a - b);
  ;
}
console.log(Math.random() * (38 - 11) + 11);

// // Paris
function getRanNum(a, b,) {
  return Math.random() * (a - b);
  ;
}
console.log(Math.random() * (38 - 20) + 20);

// // Lima
function getRanNum(a, b,) {
  return Math.random() * (a - b);
  ;
}
console.log(Math.random() * (16 - 2) + 2);



// // These are the stepts to append in Javascript 
// var divElement = document.createElement('div'); // this targets the div specifically 

// var h3Elemment = document.createElement('h2');

// h2Elment.textContent = dest1.name 

// var sectionElement = documentElementByID('profiles');
// sectionElement.appendChild(divElement);

